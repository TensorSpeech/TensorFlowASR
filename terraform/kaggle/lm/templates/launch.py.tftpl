# ---------------------------------------------------------------------------
# TensorFlowASR LM training launcher. Generated by Terraform (terraform/kaggle/lm).
# All the work is in run.sh, embedded below: this cell only writes it out and runs
# it, streaming its output. Editing on Kaggle is overwritten by the next `terraform
# apply` -- change the tfvars and re-apply instead.
# ---------------------------------------------------------------------------
import subprocess
import sys

RUN_SH = r'''
${run_sh}
'''

with open("/kaggle/working/run.sh", "w", encoding="utf-8") as handle:
    handle.write(RUN_SH.strip() + "\n")

sys.exit(subprocess.run(["bash", "/kaggle/working/run.sh"]).returncode)
