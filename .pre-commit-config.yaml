repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff-format
        entry: uv run ruff format --force-exclude
        language: system
        types: [python]
        stages: [pre-commit]
        fail_fast: true
        require_serial: true
        verbose: true
      - id: ruff-check
        name: ruff-check
        entry: uv run ruff check --fix --force-exclude
        language: system
        types: [python]
        stages: [pre-commit]
        fail_fast: true
        require_serial: true
        verbose: true
